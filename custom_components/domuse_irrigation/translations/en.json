{
  "config": {
    "step": {
      "user": {
        "title": "Domuse Irrigation",
        "description": "Set up the Domuse Irrigation integration.",
        "data": { "show_in_sidebar": "Show in sidebar" }
      }
    },
    "abort": { "already_configured": "Integration is already configured." }
  },
  "options": {
    "step": {
      "init": {
        "title": "Domuse Irrigation Options",
        "data": { "show_in_sidebar": "Show in sidebar" }
      }
    }
  }
}